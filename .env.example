```dotenv
# Required: ZeroTier network ID (16 hex characters)
ZT_NETWORK=                      # REQUIRED: e.g. 8a7f1c2e3d4b5a6f

# Optional: API key for auto-authorization (32 alphanumeric characters)
ZT_API_KEY=

# Optional: API base URL (default: https://my.zerotier.com/api)
ZT_API_URL=https://my.zerotier.com/api

# Optional: member name and description to set on the controller
ZT_MEMBER_NAME=
ZT_MEMBER_DESCRIPTION=

# Optional: timeouts (seconds)
ZT_JOIN_TIMEOUT=30
ZT_AUTHORIZE_TIMEOUT=30

# Optional: verbosity (0 = normal, 1 = verbose)
ZT_VERBOSE=0

# Optional: API retries when fetching member (useful if controller needs time to register member)
ZT_API_RETRIES=10
ZT_RETRY_INTERVAL=1

```  
